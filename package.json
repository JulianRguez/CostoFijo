{
  "name": "proyecto-fullstack",
  "private": true,
  "scripts": {
  "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
  "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npx copyfiles -u 1 \"frontend/dist/**/*\" backend/dist",
  "start": "NODE_ENV=production npm run start --prefix backend"
},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "copyfiles": "^2.4.1"
  }
}
